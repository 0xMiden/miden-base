use.agglayer::bridge_in -> bridge_in
use.miden::active_note

begin
    dropw
    # => [pad(16)]

    push.0 exec.active_note::get_inputs
    # => [num_inputs, dest_ptr, pad(16)]

    drop drop

    mem_loadw_be.0 swapw mem_loadw_be.4
    # => [INPUTS[0], INPUTS[1]]

    push.222 debug.stack drop

    call.bridge_in::update_ger
    # => []
end
